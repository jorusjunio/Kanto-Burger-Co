# TODO

- [x] Update `src/app/globals.css` to upgrade `.customer-nav` into a clearer “glass” (stronger blur, better specular highlights, Apple-like glossy sweep, smoother hover states).

- [x] Update `src/components/customer/customer-top-bar.tsx` hero variant classes to better match the upgraded glass system (less heavy shadows, consistent border/tint, improved hover micro-interactions).

- [ ] Sanity test: run dev server + check hero/light variants + hover effects + mobile sheet trigger.


